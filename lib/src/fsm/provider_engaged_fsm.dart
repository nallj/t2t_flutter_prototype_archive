enum ProviderEnagagedFsmState {
  START,
  REQUEST_INIT,
  LOCATION_INIT,
  FULL_INIT, // REQUEST_INIT + LOCATION_INIT
}

class ProviderEnagagedFsm {
  // RequestStatus _current;
  // RequestStatus _next;
}
